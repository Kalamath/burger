<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newsleep="{{devour}}">
		{{#if devour}}EAT TIME!{{else}}MEAT!{{/if}}
	</button>

	<button class="eat-burger" data-id="{{this.id}}">EAT!</button>
</li>
